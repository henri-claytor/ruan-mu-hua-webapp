# Google OAuth Client ID（非機密，前後端共用同一組值）
VITE_GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_ID=

# Google Service Account 的 JSON 金鑰內容（整包 JSON 貼成一行字串），供後端存取 Google Drive 用
GOOGLE_SERVICE_ACCOUNT_JSON=

# 簽發 session JWT 用的密鑰，任意足夠長的隨機字串即可
SESSION_JWT_SECRET=
